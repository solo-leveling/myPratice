function maxGap(numbers) {
    // var arr = [];var temp = 0;var num = 0;
    // var sort = numbers.sort(function(a,b){return b-a});
    // for(var i = 0 ; i < numbers.length;i++){
    //     ++temp;
    //     if(i != numbers.length-1){
    //     arr.push(Math.abs((numbers[i]-numbers[temp])));
    // }
    // }num = arr.sort(function(a,b){return b-a})
    // // return num[0];
    // console.log(num);
    var ans = numbers.reduce((a, b) => {
        return a + b + 0;
    })
    var ans1 = numbers.map((a,b)=>{
        return        
    })
    console.log(ans)
        //console.log(num[0]);
}
maxGap([1, 2, "asdas", 4]);